#############################################
# App & Auth
#############################################
NEXTAUTH_URL=http://localhost:3000
# Ganti dengan string acak aman (contoh: output "openssl rand -base64 32")
NEXTAUTH_SECRET=your-secret-here

#############################################
# Database (PostgreSQL / Prisma)
#############################################
# Contoh umum (sesuaikan kredensial Anda)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require
# Rekomendasi (Neon/Vercel): gunakan URL Prisma (punya parameter yang sesuai)
# POSTGRES_PRISMA_URL dapat disalin ke DATABASE_URL di host produksi
# Contoh:
# DATABASE_URL=postgresql://neondb_owner:password@ep-xxx-pooler.region.aws.neon.tech/neondb?connect_timeout=15&sslmode=require

#############################################
# Seeder Admin (opsional)
#############################################
# Jika diisi, seeder akan membuat/upgrade akun admin default
ADMIN_EMAIL=admin@brilliant-les.id
ADMIN_PASSWORD=admin123
ADMIN_NAME=Administrator

# Uploadthing (optional)
UPLOADTHING_SECRET=
UPLOADTHING_APP_ID=

# SMTP (optional for email notifications)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@brilliant-les.id
